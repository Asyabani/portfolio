import type { Metadata } from 'next';

// Work in progress — intentionally empty and kept out of search results
export const metadata: Metadata = {
  title: 'Product',
  robots: { index: false, follow: false },
};

export default function ProductPage() {
  return null;
}
